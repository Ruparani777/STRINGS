1.FORWARD ITERATOR://WE CHANGE g to G
2.REVERSE ITERATOR// SAME s To S
3.CONSTANT FORWARD ITERATOR: MODIFICATION DOESNT POSSIBLE // *it='G' cant possible (cstart and cend)
4.CONSTANT  REVERSE ITERATOR: MODIFICATION DOESNT POSSIBLE(crstart and crend)



DECLARE ITERATOR
DISPLAY OR FOR LOOP USING FUN()
PRINT

string str = "geeksforgeeks";
  
    // Declaring iterator
    std::string::iterator it;
  
    // Declaring reverse iterator
    std::string::reverse_iterator it1;
    cout<<"Str:"<<str<<"\n";
    // Displaying string
    cout << "The string using forward iterators is : ";
    for (it = str.begin(); it != str.end(); it++){
        if(it == str.begin()) *it='G';
        cout << *it;
    }
    cout << endl;
    
    //output:Geeksforgeeks
    
    
  ITERATE A STRING IN REVERSE:
  str.rbegin() to str.rend()  *it='S' same as above
  
  
  
